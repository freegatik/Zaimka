# ADR 0001: Слои и навигация

**Статус:** принято.

UIKit (пароль) + SwiftUI (основной UI). Слои: Presentation → Domain → Data. MVVM там, где есть состояние. Навигация через координаторы; DI — ручная сборка в `DIContainer`.

Импорт SwiftData из UI не развивать — через Data/репозитории.
