# Contributing

- После клона: `git config core.hooksPath .githooks`, `xcodegen generate`.
- Перед PR: `swiftformat .`, `swiftlint` (в CI — см. README / версия SwiftFormat).
- Ветки: `feature/…`, `fix/…` от `main`.

Архитектура: [docs/adr/](docs/adr/).
